# 변경 이력

이 파일이 공개 저장소 릴리스 커밋 메시지의 출처입니다.
`tools/release.mjs` 가 해당 버전 항목이 없으면 릴리스를 거부합니다.

형식: `## 0.0.0 — YYYY-MM-DD` 뒤에 무엇이 바뀌었는지.

---

## 0.4.0 — 2026-09-22

### 학습 루프가 실제로는 한 번도 돈 적이 없었습니다

0.3.0 에서 자동 기록을 붙였지만, **실제 워커로 돌려보니 다섯 군데가 끊겨 있었습니다.**
단위 테스트 373건은 그 내내 전부 초록이었습니다 — 상태 객체를 손으로 만들어 넘겨서,
실제 런에는 없는 키를 채워 놓고 검사했기 때문입니다.

| | 무엇이 끊겨 있었나 |
|---|---|
| 1 | `propose()` 를 아무도 안 부름 (0.3.0 에서 고침) |
| 2 | `signalsFrom` 이 `state.events` 를 읽음 — **존재한 적 없는 키.** 이벤트는 `.som/events.ndjson` 에만 있습니다. 재시도·에스컬레이션 신호가 영구 무효였습니다 |
| 3 | 이벤트가 task id 만 담아, 패턴이 노드 이름 대신 `task_143c…` 를 적을 뻔했습니다 |
| 4 | **`filesModified` 가 항상 빈 배열.** 아무도 워커에게 파일 보고를 시키지 않았습니다 — 배관은 끝까지 있는데 공급자가 없었습니다. **선언 밖 쓰기 가드는 한 번도 발화한 적이 없습니다** |
| 5 | 라우터가 위반을 **알리기만 하고 기록하지 않음.** 터미널에 한 줄 찍고 사라졌고, 리포트와 `signalsFrom` 은 `d.violations` 를 읽는데 아무도 넣지 않았습니다 |

실제 워커로 전 구간을 확인했습니다:

```
워커가 선언 밖 파일을 씀 → 라우터가 위반 기록 → finish() 가 패턴 생성
→ "writes-wide 는 선언한 범위 밖 파일을 고친다" (신뢰도 25)
→ 다음 브리핑에 실제로 주입됨
```

`test/e2e-learning.mjs` 가 이걸 매번 확인하고, **릴리스 게이트에 들어갔습니다.**

### Word 출력

`--emit docx` 가 생겼습니다. R&R·차터를 **회람하고 레드라인**하려면 필요했는데
없었습니다.

- 같은 IR, 같은 구조. HTML emitter 가 도는 자리를 그대로 돕니다
- 차트는 **데이터 표**가 됩니다 — SVG 를 래스터화한 흐린 그림보다 숫자 네 개가 낫습니다.
  결론(takeaway)은 표 **위에** 굵게, 화면에서와 같은 규칙입니다
- grain 캡션 · 결정 요청 · `ir_sha256` 출처 줄 전부 유지
- 테스트가 **HTML 과 대조**합니다. 픽스처와 비교하면 둘이 갈라져도 초록이니까요
- 그 테스트가 버그를 잡았습니다: 차트에서 `labels` 를 읽고 있었는데 IR 은 `categories`
  입니다 — 모든 차트가 플레이스홀더로 떨어질 뻔했습니다

### 테스트

383건 (node 240 · python 143). docx 10건이 새로 들어갔습니다.

---

## 0.3.1 — 2026-09-22

### 병렬 런이 전부 죽고 있었습니다

```
ReferenceError: pf is not defined   (conduct.mjs:662)
```

`const pf = this.preflight()` 가 `if (!plan.singleAgent) {}` **블록 안에** 선언돼
있었고, 아래 두 사용처(`pf.runtimeId` · `pf.worktreeId`)는 블록 밖이었습니다.
**wave 가 1보다 넓은 두 레시피 — `prd` 와 `build` — 가 첫 상태 저장에서 즉사**했습니다.

`f903b05` 에서 preflight 를 조건부로 옮길 때 선언만 블록 안으로 따라 들어가고
사용처는 남았습니다. **테스트 373건이 전부 초록인 채로** 나갔습니다.

### 그 구멍을 릴리스 게이트로 막았습니다

`Conduct.run()` 은 **단위 테스트가 0건**입니다. Orca 와 유료 워커가 필요해서
node 스위트 240건 어디도 부르지 않고, 실제로 지나가는 건 e2e 하나뿐인데 그건
기본 스위트에도 CI 에도 없습니다.

이제 `tools/release.mjs` 가 **릴리스마다 실제 워커로 e2e 를 돌립니다.** 실패하면
아무것도 안 나갑니다. Orca 없는 머신은 `--no-e2e` 로 건너뛸 수 있지만, 건너뛰었다고
출력에 찍힙니다.

---

## 0.3.0 — 2026-09-22

### 런이 끝나면 엔진이 스스로 패턴을 남깁니다

**학습 루프의 첫 칸이 비어 있었습니다.** 주입도 채점도 자동이었는데 **만드는
것만 수동**이라, `/som:learn` 을 직접 안 부르면 라이브러리가 영원히 비어 있었습니다.
많이 쓰셔도 패턴 0건 · 승격 0건 — "저장할 만한 게 없었다" 로 보이지만 실은
**한 번도 제안된 적이 없었던** 겁니다.

`conduct` 가 끝날 때 **직접 관측한 것**만 패턴으로 남깁니다:

| 신호 | 남기는 것 |
|---|---|
| 선언 밖 쓰기 | 그 노드의 `writes` 에 실제 고친 경로를 넣으라고 — 양쪽 다 이름으로 |
| 2회 넘게 재시도 후 성공 | 그 노드를 더 작게 쪼개라고 — 파일 목록과 함께 |
| 사람이 풀어준 에스컬레이션 | 그 입력을 인터뷰에서 미리 받으라고 |

- **엔진이 교훈을 지어내지 않습니다.** 워커가 쓴 요약문은 안 씁니다 — 이 모듈이
  측정하지 않은 산문입니다. "잘 됐다" 도 안 남깁니다 — 성공은 기준선입니다
- **게이트는 그대로입니다.** 자동 초안도 사람이 쓴 것과 같은 검증을 통과해야 합니다.
  거부가 정상이고, 그래서 초안이 노드 이름과 실제 경로를 담습니다
- **신뢰도 25 에서 시작**합니다 (사람이 적은 건 40). 스킬 승격에 평균 70 이
  필요하니 자동 관측은 여섯 번쯤 맞아야 후보가 됩니다
- 런당 최대 4건. 스무 개가 터진 런은 문제가 스무 개가 아니라 하나입니다
- 같은 관측이 반복되면 **합쳐지고 신뢰도가 오릅니다** — 줄이 두 개 되지 않습니다

### CI 가 새 테스트를 안 돌리고 있었습니다

python 테스트를 **손으로 8개 적어둔 목록**이었고, `test_write_path.py` 가 추가된
날 그 목록이 낡았습니다 — **Snowflake 쓰기 승인 28건이 아무 데서도 안 돌았는데
CI 는 초록이었습니다.** 같은 결함이 `make_images.py` 에서 이미 세 번 났고, 그래서
거기엔 "버킷에 없는 파일이 있으면 거부" 가드가 있습니다.

이제 `engine/tests/test_*.py` 를 전부 글롭으로 돌리고, `N/M passed` 를 안 내는
suite 나 suite 개수가 줄면 **잡을 실패합니다.**

---

## 0.2.2 — 2026-09-14

- `som-how-it-works.html` 에서도 "부록 — 측정 원천" 을 뺐습니다. 이 문서의 숫자는
  전부 빌드 시점 실측이고 그걸 보증하는 건 `docs/check_counts.py` 입니다 —
  파일 경로를 다시 적은 표는 섹션만 늘리고 보증은 더하지 않았습니다

---

## 0.2.1 — 2026-09-14

- 팀 소개 페이지에서 "부록 — 이 문서의 숫자" 를 뺐습니다. 이제 "시작하기" 로
  끝납니다

---

## 0.2.0 — 2026-09-14

### Snowflake 쓰기 — 차단에서 승인으로

테이블을 만들고, 값을 고치고, 쓰다 버린 객체를 지우는 건 실제 업무입니다.
막아야 하는 건 쓰기가 아니라 **아무도 안 본 쓰기** 입니다.

- `somsql write` — 무엇을 · 어디에 · 되돌릴 수 있는지를 문장으로 보여주고 멈춥니다
- 승인은 **그 문장의 sha256 앞 12자**. 한 글자만 고쳐도 무효라, 한 건에 준
  "예"가 다른 건으로 옮겨가지 않습니다
- `--reason` 필수이고 **접속 전에** 검사합니다. 빠뜨려도 SSO 팝업이 뜨지 않습니다
- 동사·대상·영향 행·승인자·사유가 `.som/ledger.ndjson` 과 `WRITE_LOG.md` 에 남습니다
- `somsql run` 에 쓰기 파일이 들어오면 거부가 아니라 exit 4 — 같은 확인 블록을
  출력하고 사람을 기다립니다
- 기존 스크립트는 **경고만 하고 통과**합니다. 이 플러그인이 생기기 전부터 팀이
  쓰던 Snowflake 스크립트가 som 설치 때문에 멈추면 안 됩니다
- `snowflake.write` 가 하드플로어에서 게이트로 (플로어 7 → 6). 어느 레벨에서도
  자동은 아니지만, 불가능하지도 않습니다

### 읽기에는 상한이 없습니다

비용은 다른 부서 소관입니다. 필요한 만큼 가져오시면 됩니다.

- **바이트 상한 · 행 상한 · statement timeout 제거.** 60초/900초 세션 타임아웃이
  정당한 긴 추출을 죽이고 있었습니다
- **LIMIT 자동 주입 제거** — 이게 가장 나빴습니다. 41만 행 추출에 1,000행을
  돌려주면 **잘린 결과가 완전한 결과와 똑같이 생깁니다.** SQL 이 쓰인 그대로
  웨어하우스에 갑니다
- 카티전 조인 · 날짜 조건 없는 대형 테이블 조회 · 과한 조인 팬아웃은 **거부에서
  안내로.** 실행 전에 알려주고 그대로 돕니다
- `--mode explore|full` 은 원장 표기로만 남고 아무것도 제한하지 않습니다
- 남은 것: EXPLAIN 스캔 견적, 24시간 결과 캐시, 원장 기록. **비용 가시화와 비용
  통제는 다른 일이고, 원치 않은 쪽은 통제였습니다**

### Orca 받는 곳

`README` · `/som:doctor` · 팀 소개 페이지에 다운로드 링크를 넣었습니다 —
https://github.com/stablyai/orca/releases (설치본의 자동 업데이트 설정에서 확인).
없어도 여섯 중 다섯이 그대로 돌고, 있으면 som 이 알아서 씁니다.

### 배운 것이 스킬이 됩니다 — 초개인화

- 반복 증명된 **패턴 묶음**이 `~/.claude/skills/som-*/SKILL.md` 로 승격됩니다.
  Claude 가 묻지 않고 읽는 자리라, som 런 밖의 평범한 대화에도 적용됩니다
- `som learn skills plan` 은 아무것도 쓰지 않고 후보와 **보류 사유**를 보여줍니다.
  `promote --yes` 로만 실제 생성
- 승격 기준은 산술입니다 — 관련 패턴 3건 · 평균 신뢰도 70 · 적용 6회 ·
  전원 1승 이상 · 맞음 ≥ 빗나감 · 전체 8개까지
- 이름은 항상 `som-` 접두사. `som-generated: true` 표식이 없으면 이름이 같아도
  건드리지 않습니다. 되돌리기는 폴더 삭제

### 이 저장소가 공개 가능해졌습니다

- 실제 Snowflake account identifier · 서비스 계정명을 예시 파일에서 제거
- 테이블 목록 · warehouse · 도메인 코드가 코드에서 **로컬 site profile**
  (`<project>/.som/registry.json`) 로 이동
- registry 가 비면 `somsql plan` 과 `/som:doctor` 가 **말합니다.** 가드가 조용히
  약해지지 않습니다
- 문서·스킬·픽스처의 실제 객체명을 예시 이름으로 교체

### 팀 소개 페이지

- 마지막의 "부록 — 이 문서의 숫자" 를 뺐습니다. 파일 경로와 행 수 표는 이 페이지를
  읽는 사람에게 의미가 없고, 하필 **마지막으로 보는 것**이었습니다. 이제 "시작하기"
  로 끝납니다. 같은 부록은 엔지니어용 `som-how-it-works.html` 에 그대로 있습니다

### 고친 결함

- `describe` 가 `INSERT INTO IDENTIFIER($t)` 의 대상을 **"INTO" 라는 테이블**로
  보고했습니다. 사람이 읽는 유일한 줄에 자신 있게 틀린 답 — 이제 못 읽으면
  못 읽었다고 하고 심각도를 올립니다
- `write_sql` 이 없는 `connect()` 를 호출해, 승인을 받은 **뒤에** 트레이스백으로
  끝났습니다. 테스트가 전부 커넥션을 주입해서 아무도 못 봤습니다
- `--reason` 누락 거부가 원장에 안 남았습니다
- `guardrails` 그림이 엔진이 하지 않는 약속을 둘 했습니다
  ("DB 쓰기는 승인해도 안 됨", "중간에 죽이지 않음" — 후자는 L3부터 자동)
- `.gitignore` 가 `.som/` 을 막아, 문서가 "커밋할 가치가 있다"고 한 패턴 파일을
  커밋할 수 없었습니다. 그 수정이 또 슬래시 앵커 버그를 만들어 런 상태 파일이
  커밋됐고, `git check-ignore` 로 실측해 다시 고쳤습니다
- `DELTA` 의 `"miss"` (−6) 는 정의·처리만 있고 호출자가 없었습니다. 삭제

### 테스트

362건 (node 229 · python 133). 공개 위생 4건, 승격 21건, 릴리스 게이트 16건, 상한 없음을 고정하는 4건이 새로 추가됐습니다.

---

## 0.1.0 — 2026-09-09

첫 내부 버전. 문서 표준(somdoc) · Snowflake 안전 계층(somsql) ·
Orca 오케스트레이션 · 모호도 게이트 · 패턴 학습 · 자율 레벨.
